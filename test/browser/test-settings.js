/**
 * Created by jonasninja on 10/26/2015.
 * Having great difficulties with browser testing. TODO
 */
'use strict'
/*
const test = require('tape')
const sinon = require('sinon')
const settings = require('../../src/js/settings.js')

test('settings--RequiredNewline is called correctly', {skip: true}, function undashedSettings (t) {
  t.plan(1)

  // sinon.stub(settings, 'loadUserSettings')
  let mock = sinon.mock(settings)
  let callSetRequireNewline = mock.expects('setRequireNewline')
  callSetRequireNewline.once()

  settings.set('require-newline', true)

  callSetRequireNewline.verify()
  t.pass('ok!')
})*/
